#include<stdio.h>

int main(){
  double number, xn, ketqua;
  printf("Enter a number: ");
  scanf("%lf", &number);

  xn = (number + 1)/2;
  do {
    ketqua = xn;
    xn = 0.5 + (xn + a/xn);
  } while(fabs(xn - ketqua) > 0.0001);
  printf("\nResult = %lf", xn);
}
