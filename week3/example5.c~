#include<stdio.h>

int main() {
  int i - 873;
  double f = 123.94536;
  char s[] = "Happy Birthday";

  printf("Using");
  printf("The address of x is %p\n\n", &x);
  printf("Total characters printed on this line is:%n", &y);
  printf("%d\n\n", y);
  printf("%d chracters were printed\n\n", y);
  printf("Printing a %% in a format control string\n");
  
  return 0;
}
